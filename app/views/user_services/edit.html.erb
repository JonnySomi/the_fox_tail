<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-sm-4">
      <div class="form-new">
        <%= image_tag("logo.png") %><h2>Edit this tracked service</h2>
                  <div class="container pt-5">
            <%= simple_form_for (@user_service) do |f| %>
                <%# <%= f.input :api_key, class: '' %>
                <%= f.input :hour_spent_per_month, placeholder: "Number of hours"  %>
                <%= f.input :price_per_month, placeholder: "Price in €" %>
                <div class="container d-flex justify-content-center text-center pb-5">
                  <div class="flex" style="width: 360px; justify-content: space-between;">
                    <%= f.submit "Edit", class:'btn'%>
                    <%= link_to 'Cancel', user_service_path(@user_service), class:'btn btn-delete' %>
                  </div>
                </div>
            <% end %>
          </div>
      </div>
    </div>
  </div>
</div>
